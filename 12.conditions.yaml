- name: conditions
  hosts: local
  connection: local
  vars:
    num: 6
  tasks:
  - name: please check the digit is less then 10 ?
    ansible.builtin.debug:
      msg: " Given Number: {{ num }} is less then 10 "
    when: num < 10
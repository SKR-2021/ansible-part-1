- name: Install and start nginx
  host: frontend
  tasks:
  - name: nginx install
    ansible.builtin.package:
      name: nginx install
      state: present

  - name: start nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes